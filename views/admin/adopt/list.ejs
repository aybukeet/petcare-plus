<%- include("../../partials/head") %>
    <%- include("../../partials/navbar") %>

        <div class="min-h-screen bg-[#fafafa] relative pb-40">
            
            <div class="absolute inset-0 z-0 pointer-events-none">
                <div
                    class="absolute top-[-10%] right-[-10%] w-[50%] h-[70%] bg-pink-50 rounded-full blur-[150px] opacity-30">
                </div>
                <div
                    class="absolute bottom-[-10%] left-[-10%] w-[50%] h-[70%] bg-indigo-50 rounded-full blur-[150px] opacity-30">
                </div>
            </div>

            <div class="max-w-7xl mx-auto pt-32 px-6 relative z-10">

               
                <div class="flex flex-col md:flex-row justify-between items-end mb-12 gap-6">
                    <div>
                        <span
                            class="inline-block px-4 py-1.5 bg-indigo-50 text-indigo-600 text-[10px] font-black uppercase tracking-[0.3em] rounded-full mb-4">
                            Yuva Yönetimi
                        </span>
                        <h1 class="text-4xl md:text-6xl font-black text-gray-900 tracking-tighter leading-none">
                            Sahiplendirme <span
                                class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-500 to-pink-500">Yönetimi</span>
                        </h1>
                    </div>

                    <a href="/admin/adopt/create"
                        class="px-8 py-4 bg-gray-900 text-white rounded-2xl font-black shadow-xl hover:bg-indigo-600 transition-all duration-300 hover:scale-105 active:scale-95 flex items-center gap-3">
                        <i class="fa-solid fa-plus"></i> Yeni Yuva İlanı
                    </a>
                </div>

                
                <div class="space-y-4">
                    <% pets.forEach(p=> { %>
                        <div
                            class="glass-card p-6 rounded-[2rem] shadow-sm hover:shadow-xl hover-lift border border-white/80 transition-all duration-300 flex flex-col md:flex-row items-center gap-6 group">

                           
                            <div
                                class="w-16 h-16 bg-indigo-50 rounded-2xl flex items-center justify-center text-indigo-500 font-black text-xl shadow-inner group-hover:bg-indigo-500 group-hover:text-white transition-colors duration-500">
                                <i class="fa-solid fa-hand-holding-heart"></i>
                            </div>

                            
                            <div class="flex-1 text-center md:text-left">
                                <h3 class="text-xl font-black text-gray-800 tracking-tight">
                                    <%= p.name %>
                                </h3>
                                <div class="flex flex-wrap justify-center md:justify-start gap-4 mt-2">
                                    <span class="flex items-center gap-2 text-xs font-bold text-gray-400">
                                        <i class="fa-solid fa-paw text-indigo-200"></i>
                                        <%= p.type %>
                                    </span>
                                    <span class="flex items-center gap-2 text-xs font-bold text-gray-400">
                                        <i class="fa-solid fa-location-dot text-indigo-200"></i>
                                        <%= p.city %>
                                    </span>
                                    <span
                                        class="px-3 py-1 bg-green-50 text-green-600 rounded-full text-[10px] font-black uppercase tracking-widest border border-green-100 flex items-center gap-2">
                                        <span class="w-2 h-2 bg-green-500 rounded-full"></span>
                                        Yayında
                                    </span>
                                </div>
                            </div>

                            
                            <div class="flex items-center gap-3">
                                <form action="/admin/adopt/delete/<%= p.id %>" method="POST"
                                    onsubmit="return confirm('Bu ilanı silmek istediğinize emin misiniz?')">
                                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                    <button
                                        class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center text-gray-400 hover:text-red-600 hover:shadow-lg transition shadow-sm border border-gray-50 font-black">
                                        <i class="fa-solid fa-trash-can"></i>
                                    </button>
                                </form>
                            </div>
                        </div>
                        <% }) %>
                </div>

                <% if (pets.length===0) { %>
                    <div class="py-20 text-center">
                        <p class="text-gray-400 italic">Henüz yuva bekleyen dostumuzun kaydı bulunmuyor... 🏠</p>
                    </div>
                    <% } %>

            </div>
        </div>

        <%- include("../../partials/footer") %>