<%- include('../partials/head') %>
    <%- include('../partials/navbar') %>

        <div class="p-6">
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-2xl font-bold text-gray-800">Kayıp İlanı Geri Bildirimleri</h1>
            </div>

            <div class="bg-white rounded-xl shadow-sm overflow-hidden border border-gray-100">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="bg-gray-50 border-b border-gray-100 text-xs text-gray-500 uppercase">
                            <th class="p-4 font-semibold">Bildirim Tarihi</th>
                            <th class="p-4 font-semibold">Kayıp Hayvan</th>
                            <th class="p-4 font-semibold">Gören Kişi</th>
                            <th class="p-4 font-semibold">Konum / Zaman</th>
                            <th class="p-4 font-semibold">Mesaj</th>
                            <th class="p-4 font-semibold">Durum</th>
                            <th class="p-4 font-semibold text-right">İşlem</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        <% sightings.forEach(req=> { %>
                            <tr class="hover:bg-gray-50 transition">
                                <td class="p-4 text-sm text-gray-500">
                                    <%= new Date(req.created_at).toLocaleDateString("tr-TR") %>
                                </td>
                                <td class="p-4">
                                    <span class="font-medium text-blue-600">
                                        <%= req.pet_name %>
                                    </span>
                                </td>
                                <td class="p-4">
                                    <div class="font-semibold text-gray-800">
                                        <%= req.finder_name %>
                                    </div>
                                    <div class="text-xs text-gray-500">
                                        <%= req.finder_contact %>
                                    </div>
                                </td>
                                <td class="p-4 text-sm">
                                    <div class="font-bold text-gray-700">
                                        <%= req.location_found %>
                                    </div>
                                    <div class="text-gray-500 text-xs">
                                        <%= new Date(req.time_found).toLocaleString("tr-TR") %>
                                    </div>
                                </td>
                                <td class="p-4 text-sm text-gray-600 max-w-xs truncate" title="<%= req.message %>">
                                    <%= req.message %>
                                </td>
                                <td class="p-4">
                                    <% if (req.status==='pending' ) { %>
                                        <span
                                            class="px-2 py-1 text-xs font-bold rounded-full bg-yellow-100 text-yellow-700">Bekliyor</span>
                                        <% } else if (req.status==='approved' ) { %>
                                            <span
                                                class="px-2 py-1 text-xs font-bold rounded-full bg-green-100 text-green-700">İletildi</span>
                                            <% } else { %>
                                                <span
                                                    class="px-2 py-1 text-xs font-bold rounded-full bg-red-100 text-red-700">Reddedildi</span>
                                                <% } %>
                                </td>
                                <td class="p-4 text-right space-x-2">
                                    <% if (req.status==='pending' ) { %>
                                        <form action="/admin/lost/sightings" method="POST" class="inline-block">
                                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                            <input type="hidden" name="id" value="<%= req.id %>">
                                            <input type="hidden" name="status" value="approved">
                                            <button
                                                class="text-green-500 hover:text-green-700 font-semibold text-sm transition"
                                                title="Onayla / İletildi İşaretle">
                                                <i class="fa-solid fa-check"></i>
                                            </button>
                                        </form>
                                        <form action="/admin/lost/sightings" method="POST" class="inline-block">
                                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                            <input type="hidden" name="id" value="<%= req.id %>">
                                            <input type="hidden" name="status" value="rejected">
                                            <button
                                                class="text-red-400 hover:text-red-600 font-semibold text-sm transition"
                                                title="Reddet">
                                                <i class="fa-solid fa-xmark"></i>
                                            </button>
                                        </form>
                                        <% } else { %>
                                            <span class="text-gray-300">-</span>
                                            <% } %>
                                </td>
                            </tr>
                            <% }) %>
                                <% if (sightings.length===0) { %>
                                    <tr>
                                        <td colspan="7" class="p-8 text-center text-gray-400">
                                            Henüz bildirim yok.
                                        </td>
                                    </tr>
                                    <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        <%- include('../partials/footer') %>