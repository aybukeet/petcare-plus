<%- include("../partials/head") %>
    <%- include("../partials/navbar") %>

        <div class="min-h-screen bg-[#fafafa] relative pb-40">
            <div class="max-w-7xl mx-auto pt-32 px-6">

                
                <div class="flex flex-col md:flex-row justify-between items-end mb-12 gap-6">
                    <div>
                        <span
                            class="inline-block px-4 py-1.5 bg-pink-50 text-pink-600 text-[10px] font-black uppercase tracking-[0.3em] rounded-full mb-4">
                            Katalog Yönetimi
                        </span>
                        <h1 class="text-4xl md:text-6xl font-black text-gray-900 tracking-tighter leading-none">
                            Hayvan <span
                                class="text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-rose-600">Listesi</span>
                        </h1>
                    </div>

                    <a href="/admin/pets/add"
                        class="px-8 py-4 bg-gray-900 text-white rounded-2xl font-black shadow-xl hover:bg-pink-600 transition-all duration-300 hover:scale-105 active:scale-95 flex items-center gap-3">
                        <i class="fa-solid fa-plus font-black"></i> Yeni Kayıt Ekle
                    </a>
                </div>

                
                <div
                    class="glass-card p-4 rounded-3xl mb-10 flex flex-col md:flex-row gap-4 border border-white/50 shadow-sm">
                    <div class="flex-1 relative">
                        <i
                            class="fa-solid fa-magnifying-glass absolute left-5 top-1/2 -translate-y-1/2 text-gray-300"></i>
                        <input type="text" placeholder="İsim veya Tür Ara..."
                            class="w-full bg-white/50 border-none rounded-2xl pl-12 py-3 text-sm font-bold text-gray-700 outline-none focus:ring-2 focus:ring-pink-100 transition">
                    </div>
                    <div class="flex gap-2">
                        <button
                            class="px-6 py-3 bg-white rounded-2xl text-xs font-black text-gray-400 hover:text-pink-500 transition">Filtrele</button>
                        <button
                            class="px-6 py-3 bg-white rounded-2xl text-xs font-black text-gray-400 hover:text-pink-500 transition">Dışa
                            Aktar</button>
                    </div>
                </div>

                
                <div class="space-y-4">
                    <% pets.forEach(p=> { %>
                        <div
                            class="glass-card p-6 rounded-[2rem] shadow-sm hover:shadow-xl hover-lift border border-white/80 transition-all duration-300 flex flex-col md:flex-row items-center gap-6 group">

                           
                            <div
                                class="w-16 h-16 bg-pink-50 rounded-2xl flex items-center justify-center text-pink-600 font-black text-xl shadow-inner group-hover:bg-pink-500 group-hover:text-white transition-colors duration-500">
                                #<%= p.id %>
                            </div>

                           
                            <div class="flex-1 text-center md:text-left">
                                <h3 class="text-xl font-black text-gray-800 tracking-tight">
                                    <%= p.name %>
                                </h3>
                                <div class="flex flex-wrap justify-center md:justify-start gap-4 mt-2">
                                    <span class="flex items-center gap-2 text-xs font-bold text-gray-400">
                                        <i class="fa-solid fa-paw text-pink-200"></i>
                                        <%= p.type %>
                                    </span>
                                    <span class="flex items-center gap-2 text-xs font-bold text-gray-400">
                                        <i class="fa-solid fa-location-dot text-pink-200"></i>
                                        <%= p.city %>
                                    </span>
                                    <span
                                        class="px-3 py-1 bg-green-50 text-green-600 rounded-full text-[10px] font-black uppercase tracking-widest border border-green-100">Aktif</span>
                                </div>
                            </div>

                            
                            <div class="flex items-center gap-3">
                                <a href="/admin/pets/edit/<%= p.id %>"
                                    class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center text-gray-400 hover:text-indigo-600 hover:shadow-lg transition shadow-sm border border-gray-50">
                                    <i class="fa-solid fa-pen-to-square"></i>
                                </a>
                                <form action="/admin/pets/delete/<%= p.id %>" method="POST"
                                    onsubmit="return confirm('Bu kaydı silmek istediğinize emin misiniz?')">
                                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                    <button
                                        class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center text-gray-400 hover:text-red-600 hover:shadow-lg transition shadow-sm border border-gray-50">
                                        <i class="fa-solid fa-trash-can"></i>
                                    </button>
                                </form>
                            </div>
                        </div>
                        <% }) %>
                </div>

                <% if (pets.length===0) { %>
                    <div class="py-20 text-center">
                        <p class="text-gray-400 italic">Henüz kayıtlı bir dostumuz bulunmuyor... 🐾</p>
                    </div>
                    <% } %>

            </div>
        </div>

        <%- include("../partials/footer") %>