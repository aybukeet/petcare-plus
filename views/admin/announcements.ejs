<%- include("../partials/head") %>
    <%- include("../partials/navbar") %>

        <div class="min-h-screen bg-[#fafafa] relative pb-40">
            <div class="max-w-7xl mx-auto pt-32 px-6">

                <!-- HEADER -->
                <div class="flex flex-col md:flex-row justify-between items-end mb-12 gap-6">
                    <div>
                        <span
                            class="inline-block px-4 py-1.5 bg-teal-50 text-teal-600 text-[10px] font-black uppercase tracking-[0.3em] rounded-full mb-4">
                            İçerik Yönetimi
                        </span>
                        <h1 class="text-4xl md:text-6xl font-black text-gray-900 tracking-tighter leading-none">
                            Duyuru <span
                                class="text-transparent bg-clip-text bg-gradient-to-r from-teal-500 to-emerald-600">Listesi</span>
                        </h1>
                    </div>

                    <a href="/admin/announcements/new"
                        class="px-8 py-4 bg-gray-900 text-white rounded-2xl font-black shadow-xl hover:bg-teal-600 transition-all duration-300 hover:scale-105 active:scale-95 flex items-center gap-3">
                        <i class="fa-solid fa-bullhorn rotate-[-12deg]"></i> Yeni Duyuru Yayınla
                    </a>
                </div>

                <!-- TILED LIST -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <% announcements.forEach(item=> { %>
                        <div
                            class="glass-card p-8 rounded-[2.5rem] shadow-sm hover:shadow-xl hover-lift border border-white/80 transition-all duration-300 flex flex-col justify-between group">
                            <div>
                                <div class="flex items-center justify-between mb-6">
                                    <span class="text-[10px] font-black uppercase tracking-[0.2em] text-teal-400">#<%=
                                            item.id %></span>
                                    <span
                                        class="px-4 py-1.5 bg-gray-50 text-gray-400 rounded-full text-[10px] font-black uppercase tracking-widest">
                                        <%= item.date %>
                                    </span>
                                </div>
                                <h3
                                    class="text-2xl font-black text-gray-800 tracking-tighter mb-4 group-hover:text-teal-600 transition">
                                    <%= item.title %>
                                </h3>
                                <p class="text-gray-400 font-medium text-sm line-clamp-2 leading-relaxed mb-8">
                                    <%= item.description %>
                                </p>
                            </div>

                            <div class="flex items-center justify-between pt-6 border-t border-gray-50">
                                <div class="flex gap-2">
                                    <!-- In a real app we'd have edit here too, for now let's keep it consistent -->
                                    <span
                                        class="text-[10px] font-black text-teal-500/50 uppercase tracking-widest">Global
                                        Yayın</span>
                                </div>
                                <form action="/admin/announcements/delete/<%= item.id %>" method="POST"
                                    onsubmit="return confirm('Bu duyuruyu silmek istediğinize emin misiniz?')">
                                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                    <button
                                        class="w-12 h-12 bg-red-50 text-red-400 rounded-2xl flex items-center justify-center hover:bg-red-600 hover:text-white transition-all shadow-sm">
                                        <i class="fa-solid fa-trash-can"></i>
                                    </button>
                                </form>
                            </div>
                        </div>
                        <% }) %>
                </div>

                <% if (announcements.length===0) { %>
                    <div class="py-20 text-center">
                        <p class="text-gray-400 italic">Şu an yayında olan bir duyuru bulunmuyor... 📢</p>
                    </div>
                    <% } %>

            </div>
        </div>

        <%- include("../partials/footer") %>