<%- include("../partials/head") %>
    <%- include("../partials/navbar") %>

        <div class="min-h-screen">
            <!-- PREMIUM HERO HEADER (Urgent / Caring Theme) -->
            <header class="relative pt-32 pb-48 overflow-hidden bg-gradient-to-br from-orange-50 via-white to-red-50">
                <div class="absolute inset-0 z-0">
                    <div
                        class="absolute top-[-10%] left-[-10%] w-[40%] h-[60%] bg-orange-100 rounded-full blur-[120px] opacity-40 animate-pulse">
                    </div>
                    <div class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[60%] bg-red-100 rounded-full blur-[120px] opacity-40 animate-pulse"
                        style="animation-delay: 1s;"></div>
                </div>

                <div class="relative z-10 max-w-7xl mx-auto px-6 text-center">
                    <span
                        class="inline-block px-4 py-1.5 bg-orange-100 text-orange-600 text-xs font-black uppercase tracking-[0.2em] rounded-full mb-6 badge-pulse"
                        style="animation: pulse-orange 2s infinite;">
                        Yardımlaşma Zamanı
                    </span>
                    <style>
                        @keyframes pulse-orange {
                            0% {
                                box-shadow: 0 0 0 0 rgba(249, 115, 22, 0.4);
                            }

                            70% {
                                box-shadow: 0 0 0 8px rgba(249, 115, 22, 0);
                            }

                            100% {
                                box-shadow: 0 0 0 0 rgba(249, 115, 22, 0);
                            }
                        }
                    </style>
                    <h1 class="text-5xl md:text-7xl font-black text-gray-900 tracking-tight leading-none mb-6">
                        Onları Yuvalarına <br> <span
                            class="text-transparent bg-clip-text bg-gradient-to-r from-orange-500 to-red-600">Kavuşturalım</span>
                    </h1>
                    <p class="text-xl text-gray-500 max-w-2xl mx-auto leading-relaxed">
                        Kaybolan dostlarımızı bulmak için topluluğumuzla el ele veriyoruz. Her ilan bir umut ışığıdır.
                        💛
                    </p>
                </div>
            </header>

            <!-- FLOATING FILTER BAR -->
            <section class="relative z-20 -mt-20 max-w-4xl mx-auto px-6">
                <div class="glass-card rounded-[32px] p-4 shadow-2xl border border-white/50">
                    <form action="/lost" method="GET" class="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <div class="relative">
                            <select name="type"
                                class="w-full bg-white/50 px-6 py-4 rounded-2xl border-none text-gray-700 font-bold focus:ring-2 focus:ring-orange-300 transition">
                                <option value="">Tüm Türler</option>
                                <option value="dog" <%=locals.selectedType==='dog' ? 'selected' : '' %>>Köpek</option>
                                <option value="cat" <%=locals.selectedType==='cat' ? 'selected' : '' %>>Kedi</option>
                            </select>
                        </div>

                        <div class="relative">
                            <input type="text" name="city" placeholder="Hangi Şehirde?"
                                class="w-full bg-white/50 px-6 py-4 rounded-2xl border-none text-gray-700 font-bold focus:ring-2 focus:ring-orange-300 transition">
                        </div>

                        <button
                            class="w-full bg-gradient-to-br from-orange-500 to-red-600 hover:shadow-orange-200 text-white font-black py-4 rounded-2xl shadow-xl transition-all duration-300 hover:scale-[1.02] active:scale-95 flex items-center justify-center gap-2">
                            <i class="fa-solid fa-search"></i> İlanları Ara
                        </button>
                    </form>
                </div>
            </section>

            <!-- LOST PET LIST -->
            <main class="max-w-7xl mx-auto px-6 py-20 pb-40">
                <div class="flex items-center justify-between mb-12">
                    <h3 class="text-2xl font-black text-gray-800 tracking-tighter">Aktif İlanlar</h3>
                    <span class="text-sm font-bold text-gray-400">
                        <%= lostPets.length %> İlan Bildirildi
                    </span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
                    <% lostPets.forEach(pet=> { %>
                        <%- include("../partials/lostCard", { id: pet.id, img: pet.image, name: pet.name, city:
                            pet.city, date: pet.date_lost, type: pet.type, slug: pet.slug }) %>
                            <% }) %>
                </div>

                <% if (lostPets.length===0) { %>
                    <div class="py-32 text-center flex flex-col items-center gap-6">
                        <div
                            class="w-32 h-32 bg-orange-50 rounded-full flex items-center justify-center text-orange-300 text-6xl animate-pulse">
                            <i class="fa-solid fa-heart-circle-check"></i>
                        </div>
                        <h3 class="text-3xl font-black text-gray-800">Şu an her şey yolunda!</h3>
                        <p class="text-gray-500 max-w-sm text-lg">
                            Aradığın kriterlerde aktif bir kayıp ilanı bulunamadı. Bu harika bir haber! 🐾
                        </p>
                        <a href="/lost"
                            class="text-orange-600 font-black border-b-2 border-orange-200 hover:border-orange-500 transition-all">Filtreleri
                            Temizle</a>
                    </div>
                    <% } %>
            </main>
        </div>

        <%- include("../partials/footer") %>