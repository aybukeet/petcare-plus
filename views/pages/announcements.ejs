<%- include("../partials/head") %>
    <%- include("../partials/navbar") %>

        <div class="min-h-screen bg-[#fafafa]">
            
            <header class="relative pt-32 pb-44 overflow-hidden bg-white">
                
                <div class="absolute inset-0">
                    <div
                        class="absolute top-[-10%] right-[-10%] w-[40%] h-[60%] bg-purple-50 rounded-full blur-[120px] opacity-40">
                    </div>
                    <div
                        class="absolute bottom-[-10%] left-[-10%] w-[40%] h-[60%] bg-pink-50 rounded-full blur-[120px] opacity-40">
                    </div>
                </div>

                <div class="relative z-10 max-w-7xl mx-auto px-6 text-center">
                    <span
                        class="inline-block px-4 py-1.5 bg-purple-50 text-purple-600 text-[10px] font-black uppercase tracking-[0.3em] rounded-full mb-6">
                        Haberler & Duyurular
                    </span>
                    <h1 class="text-5xl md:text-7xl font-black text-gray-900 tracking-tighter leading-none mb-6">
                        PetCare <span
                            class="text-transparent bg-clip-text bg-gradient-to-r from-purple-600 to-pink-500">Dünyasından</span>
                    </h1>
                    <p class="text-xl text-gray-400 max-w-2xl mx-auto font-medium">
                        Sistem güncellemeleri, etkinlikler ve sevimli dostlarımızla ilgili en yeni haberleri buradan
                        takip edebilirsiniz. 📢
                    </p>
                </div>
            </header>

          
            <main class="max-w-7xl mx-auto px-6 -mt-20 pb-40 grid grid-cols-1 lg:grid-cols-12 gap-10">

                
                <aside class="lg:col-span-4 space-y-4">
                    <div class="glass-card rounded-[2.5rem] p-8 shadow-xl border border-white/50">
                        <h2 class="text-xl font-black text-gray-800 mb-6 flex items-center gap-2">
                            <i class="fa-solid fa-list-ul text-purple-400"></i> Tüm Haberler
                        </h2>

                        <div class="space-y-3">
                            <% announcements.forEach(item=> { %>
                                <a href="/announcements/<%= item.slug %>"
                                    class="group block p-5 rounded-2xl transition-all duration-300 border-2 
                           <%= selected && selected.id === item.id 
                                ? 'bg-purple-600 border-purple-600 text-white shadow-lg shadow-purple-200 -translate-y-1' 
                                : 'bg-white border-transparent hover:border-purple-100 hover:bg-purple-50 text-gray-700' %>">

                                    <div class="flex justify-between items-start gap-3">
                                        <h3
                                            class="font-bold text-sm leading-tight group-hover:text-purple-600 transition <%= selected && selected.id === item.id ? 'text-white' : '' %>">
                                            <%= item.title %>
                                        </h3>
                                        <div class="text-[10px] font-black opacity-40 uppercase whitespace-nowrap">
                                            <%= (item.date && typeof item.date==='string' && item.date.includes('-')) ?
                                                item.date.split('-').reverse().join('.') : (item.date ? new
                                                Date(item.date).toLocaleDateString('tr-TR') : '' ) %>
                                        </div>
                                    </div>
                                </a>
                                <% }) %>
                        </div>
                    </div>

                    
                    <div
                        class="bg-gradient-to-br from-indigo-500 to-purple-600 rounded-[2.5rem] p-8 text-white shadow-xl">
                        <p class="text-[10px] font-black uppercase tracking-widest opacity-60 mb-2">Bültene Kayıt Olun
                        </p>
                        <h4 class="text-lg font-bold mb-4">Gelişmelerden İlk Sizin Haberiniz Olsun</h4>
                        <div class="flex gap-2">
                            <input type="text" placeholder="E-posta"
                                class="w-full bg-white/20 border-none rounded-xl px-4 text-sm placeholder:text-white/50">
                            <button
                                class="bg-white text-purple-600 px-4 py-2 rounded-xl font-bold text-sm shadow-lg">Katıl</button>
                        </div>
                    </div>
                </aside>

               
                <section class="lg:col-span-8">
                    <% if (selected) { %>
                        <article
                            class="bg-white rounded-[3rem] p-10 md:p-16 shadow-2xl border border-gray-50 min-h-[600px] flex flex-col">
                            <div class="mb-10">
                                <span
                                    class="inline-flex items-center gap-2 px-4 py-2 bg-purple-50 text-purple-600 rounded-full text-xs font-black uppercase tracking-widest mb-6">
                                    <i class="fa-solid fa-calendar-day"></i>
                                    <%= (selected.date && typeof selected.date==='string' &&
                                        selected.date.includes('-')) ? selected.date.split('-').reverse().join('.') :
                                        (selected.date ? new Date(selected.date).toLocaleDateString('tr-TR', {
                                        day: 'numeric' , month: 'long' , year: 'numeric' }) : '' ) %>
                                </span>
                                <h1
                                    class="text-4xl md:text-6xl font-black text-gray-900 leading-[1.1] tracking-tight mb-8">
                                    <%= selected.title %>
                                </h1>
                                <div class="w-20 h-2 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full"></div>
                            </div>

                            <div class="flex-1">
                                <p
                                    class="text-xl md:text-2xl text-gray-700 leading-relaxed font-medium whitespace-pre-line first-letter:text-6xl first-letter:font-black first-letter:text-purple-600 first-letter:mr-3 first-letter:float-left">
                                    <%= selected.description %>
                                </p>
                            </div>

                            <!-- Article Footer -->
                            <div class="mt-20 pt-10 border-t border-gray-100 flex justify-between items-center">
                                <div class="flex items-center gap-4">
                                    <img src="/images/paw.png" class="w-10 h-10 opacity-20" />
                                    <div>
                                        <p class="text-xs font-black text-gray-400 uppercase tracking-widest">Yayınlayan
                                        </p>
                                        <p class="text-sm font-bold text-gray-800">PetCare Plus Haber Merkezi</p>
                                    </div>
                                </div>
                                <div class="flex gap-3">
                                    <button
                                        class="w-10 h-10 rounded-full bg-gray-50 text-gray-400 hover:bg-purple-100 hover:text-purple-600 transition flex items-center justify-center">
                                        <i class="fa-brands fa-twitter"></i>
                                    </button>
                                    <button
                                        class="w-10 h-10 rounded-full bg-gray-50 text-gray-400 hover:bg-purple-100 hover:text-purple-600 transition flex items-center justify-center">
                                        <i class="fa-brands fa-facebook"></i>
                                    </button>
                                </div>
                            </div>
                        </article>
                        <% } else { %>
                            <div
                                class="bg-white/50 backdrop-blur-sm rounded-[3rem] border-4 border-dashed border-gray-100 h-full min-h-[600px] flex flex-col items-center justify-center p-20 text-center">
                                <div
                                    class="w-32 h-32 bg-purple-50 rounded-full flex items-center justify-center text-purple-200 text-6xl mb-6">
                                    <i class="fa-solid fa-newspaper animate-bounce"></i>
                                </div>
                                <h2 class="text-3xl font-black text-gray-800 mb-4">Gelişmeleri Takip Edin</h2>
                                <p class="text-gray-400 text-lg max-w-sm leading-relaxed">
                                    Topluluğumuzdaki en güncel haberleri okumak için soldaki listeden bir duyuru seçin.
                                    📢
                                </p>
                            </div>
                            <% } %>
                </section>

            </main>
        </div>

        <%- include("../partials/footer") %>