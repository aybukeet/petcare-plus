<%- include("../partials/head") %>
    <%- include("../partials/navbar") %>

        <div class="min-h-screen">
            <!-- PREMIUM HERO HEADER -->
            <header class="relative pt-32 pb-48 overflow-hidden bg-gradient-to-br from-pink-50 via-white to-blue-50">
                <div class="absolute inset-0 z-0">
                    <div
                        class="absolute top-[-10%] left-[-10%] w-[40%] h-[60%] bg-pink-100 rounded-full blur-[120px] opacity-40 animate-pulse">
                    </div>
                    <div class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[60%] bg-blue-100 rounded-full blur-[120px] opacity-40 animate-pulse"
                        style="animation-delay: 1s;"></div>
                </div>

                <div class="relative z-10 max-w-7xl mx-auto px-6 text-center">
                    <span
                        class="inline-block px-4 py-1.5 bg-pink-100 text-pink-600 text-xs font-black uppercase tracking-[0.2em] rounded-full mb-6 badge-pulse">
                        Yeni Bir Başlangıç
                    </span>
                    <h1 class="text-5xl md:text-7xl font-black text-gray-900 tracking-tight leading-none mb-6">
                        Mükemmel Evcil <br> <span
                            class="text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-violet-600">Hayvanınızı
                            Bulun</span>
                    </h1>
                    <p class="text-xl text-gray-500 max-w-2xl mx-auto leading-relaxed">
                        Sevgi dolu bir yuva arayan dostlarımız seni bekliyor. Onların hayatını değiştirmek senin elinde.
                    </p>
                </div>
            </header>

            <!-- FLOATING FILTER BAR -->
            <section class="relative z-20 -mt-20 max-w-5xl mx-auto px-6">
                <div class="glass-card rounded-[32px] p-4 shadow-2xl border border-white/50">
                    <form action="/adopt" method="GET" class="grid grid-cols-1 md:grid-cols-4 gap-4">
                        <div class="relative">
                            <select name="type"
                                class="w-full bg-white/50 px-6 py-4 rounded-2xl border-none text-gray-700 font-bold focus:ring-2 focus:ring-pink-300 transition">
                                <option value="">Tüm Türler</option>
                                <option value="dog" <%=locals.selectedType==='dog' ? 'selected' : '' %>>Köpek</option>
                                <option value="cat" <%=locals.selectedType==='cat' ? 'selected' : '' %>>Kedi</option>
                            </select>
                        </div>

                        <div class="relative">
                            <select name="age"
                                class="w-full bg-white/50 px-6 py-4 rounded-2xl border-none text-gray-700 font-bold focus:ring-2 focus:ring-pink-300 transition">
                                <option value="">Tüm Yaşlar</option>
                                <% [1,2,3].forEach(a=> { %>
                                    <option value="<%= a %>">
                                        <%= a %> Yaş
                                    </option>
                                    <% }) %>
                                        <option value="+3">3+ Yaş</option>
                            </select>
                        </div>

                        <div class="relative">
                            <input type="text" name="city" placeholder="Şehir Ara..."
                                class="w-full bg-white/50 px-6 py-4 rounded-2xl border-none text-gray-700 font-bold focus:ring-2 focus:ring-pink-300 transition">
                        </div>

                        <button
                            class="w-full bg-gradient-to-br from-gray-900 to-black hover:from-pink-600 hover:to-pink-500 text-white font-black py-4 rounded-2xl shadow-xl transition-all duration-300 hover:scale-[1.02] active:scale-95 flex items-center justify-center gap-2">
                            <i class="fa-solid fa-magnifying-glass"></i> Filtrele
                        </button>
                    </form>
                </div>
            </section>

            <!-- PET LIST -->
            <main class="max-w-7xl mx-auto px-6 py-20 pb-40">
                <div class="flex items-center justify-between mb-12">
                    <h3 class="text-2xl font-black text-gray-800 tracking-tighter">
                        <%= pets.length> 0 ? 'Sana Uygun Dostlarımız' : '' %>
                    </h3>
                    <span class="text-sm font-bold text-gray-400">
                        <%= pets.length %> İlan Listeleniyor
                    </span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
                    <% pets.forEach(pet=> { %>
                        <%- include("../partials/petCard", { id: pet.id, img: pet.image, name: pet.name, age: pet.age
                            + " yaşında" , city: pet.city, type: pet.type, slug: pet.slug }) %>
                            <% }) %>
                </div>

                <% if (pets.length===0) { %>
                    <div class="py-32 text-center flex flex-col items-center gap-6">
                        <div
                            class="w-32 h-32 bg-pink-50 rounded-full flex items-center justify-center text-pink-300 text-6xl animate-bounce">
                            <i class="fa-solid fa-paw"></i>
                        </div>
                        <h3 class="text-3xl font-black text-gray-800">Henüz bir dostumuz yok!</h3>
                        <p class="text-gray-500 max-w-md text-lg">
                            Seçtiğin kriterlere uygun bir hayvan bulamadık. Filtreleri değiştirerek yeniden aramayı dene
                            🐾
                        </p>
                        <a href="/adopt"
                            class="text-pink-600 font-black border-b-2 border-pink-200 hover:border-pink-500 transition-all">Tüm
                            İlanları Gör</a>
                    </div>
                    <% } %>
            </main>
        </div>

        <%- include("../partials/footer") %>