<%- include('../partials/head') %>
    <%- include('../partials/navbar') %>

        <div class="container mx-auto px-4 py-12 flex flex-col items-center justify-center min-h-[60vh]">
            <div class="bg-white p-8 rounded-2xl shadow-xl text-center max-w-md w-full border border-green-100">
                <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6">
                    <i class="fa-solid fa-check text-4xl text-green-500"></i>
                </div>

                <h1 class="text-2xl font-bold text-gray-800 mb-4">
                    <%= title %>
                </h1>
                <p class="text-gray-600 mb-8">
                    <%= message %>
                </p>

                <a href="<%= redirectUrl %>"
                    class="inline-block w-full bg-green-500 text-white font-semibold py-3 rounded-xl hover:bg-green-600 transition shadow-lg hover:shadow-xl hover:-translate-y-1">
                    Tamam
                </a>
            </div>
        </div>

        <%- include('../partials/footer') %>