<%- include("../partials/head") %>
    <%- include("../partials/navbar") %>

        <div class="min-h-screen relative flex items-center justify-center overflow-hidden bg-white px-6 py-20">
            <!-- BACKGROUND GLOWS (Immersive Atmosphere) -->
            <div class="absolute inset-0 z-0 pointer-events-none">
                <div
                    class="absolute top-[-20%] left-[-10%] w-[60%] h-[80%] bg-pink-100 rounded-full blur-[150px] opacity-40 animate-pulse">
                </div>
                <div class="absolute bottom-[-20%] right-[-10%] w-[60%] h-[80%] bg-blue-50 rounded-full blur-[150px] opacity-40 animate-pulse"
                    style="animation-delay: 1.5s;"></div>
            </div>

            <!-- LOGIN CARD -->
            <div class="relative z-10 w-full max-w-lg">
                <div
                    class="glass-card rounded-[3rem] p-10 md:p-16 shadow-2xl border border-white/50 relative overflow-hidden">

                    <!-- Playful Icon Decoration -->
                    <div class="absolute -top-10 -right-10 w-40 h-40 bg-pink-50 rounded-full blur-3xl opacity-50"></div>

                    <div class="text-center mb-10">
                        <div
                            class="w-20 h-20 bg-pink-500 text-white rounded-[2rem] flex items-center justify-center text-3xl mx-auto mb-6 shadow-lg shadow-pink-200 rotate-6 hover:rotate-0 transition-transform duration-500">
                            <i class="fa-solid fa-key"></i>
                        </div>
                        <h1 class="text-4xl font-black text-gray-900 tracking-tight mb-2">Hoş Geldiniz 🐾</h1>
                        <p class="text-gray-500 font-medium">Dostlarımızı görmek için tekrar giriş yapın.</p>
                    </div>

                    <% if (message) { %>
                        <div
                            class="mb-8 p-4 rounded-2xl animate-shake <%= message.type === 'error' ? 'bg-red-50 text-red-600 border border-red-100' : 'bg-green-50 text-green-600 border border-green-100' %> text-sm font-bold flex items-center gap-3">
                            <i
                                class="fa-solid <%= message.type === 'error' ? 'fa-circle-exclamation' : 'fa-circle-check' %>"></i>
                            <%= message.text %>
                        </div>
                        <% } %>

                            <form action="/auth/login" method="POST" class="space-y-6">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                                <div class="space-y-4">
                                    <div class="relative group">
                                        <label
                                            class="block text-[10px] font-black uppercase tracking-widest text-gray-400 mb-2 ml-1">E-posta
                                            Adresiniz</label>
                                        <div class="relative">
                                            <i
                                                class="fa-solid fa-envelope absolute left-5 top-1/2 -translate-y-1/2 text-gray-300 group-focus-within:text-pink-500 transition"></i>
                                            <input type="email" name="email" placeholder="ornek@mail.com" required
                                                class="w-full bg-white/80 backdrop-blur-sm border-2 border-transparent px-12 py-4 rounded-2xl text-lg font-bold text-gray-800 focus:border-pink-300 focus:bg-white shadow-sm focus:ring-0 transition">
                                        </div>
                                    </div>

                                    <div class="relative group">
                                        <label
                                            class="block text-[10px] font-black uppercase tracking-widest text-gray-400 mb-2 ml-1">Şifreniz</label>
                                        <div class="relative">
                                            <i
                                                class="fa-solid fa-lock absolute left-5 top-1/2 -translate-y-1/2 text-gray-300 group-focus-within:text-pink-500 transition"></i>
                                            <input type="password" name="password" placeholder="••••••••" required
                                                class="w-full bg-white/80 backdrop-blur-sm border-2 border-transparent px-12 py-4 rounded-2xl text-lg font-bold text-gray-800 focus:border-pink-300 focus:bg-white shadow-sm focus:ring-0 transition">
                                        </div>
                                    </div>
                                </div>

                                <div class="flex items-center justify-between px-1">
                                    <label class="flex items-center gap-2 cursor-pointer group">
                                        <input type="checkbox"
                                            class="w-4 h-4 rounded border-gray-300 text-pink-500 focus:ring-pink-500 transition">
                                        <span
                                            class="text-sm font-bold text-gray-400 group-hover:text-gray-600 transition">Beni
                                            Hatırla</span>
                                    </label>
                                    <a href="/auth/forgot-password"
                                        class="text-sm font-black text-pink-500 hover:text-pink-600 transition">Şifremi
                                        Unuttum</a>
                                </div>

                                <button
                                    class="w-full bg-gradient-to-r from-gray-900 to-black text-white py-5 rounded-[2rem] font-black text-lg shadow-xl hover:from-pink-600 hover:to-pink-500 hover:scale-[1.02] active:scale-95 transition-all duration-300">
                                    Giriş Yap <i class="fa-solid fa-arrow-right-to-bracket ml-2 opacity-50"></i>
                                </button>
                            </form>

                            <div class="mt-12 pt-8 border-t border-gray-100/50 text-center">
                                <p class="text-gray-500 font-medium">Hesabınız yok mu?
                                    <a href="/auth/register"
                                        class="text-pink-500 font-black hover:border-b-2 border-pink-200 transition-all">Şimdi
                                        Kaydolun</a>
                                </p>
                            </div>
                </div>
            </div>
        </div>

        <%- include("../partials/footer") %>