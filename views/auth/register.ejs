<%- include("../partials/head") %>
    <%- include("../partials/navbar") %>

        <div class="min-h-screen relative flex items-center justify-center overflow-hidden bg-white px-6 py-20">
           
            <div class="absolute inset-0 z-0 pointer-events-none">
                <div
                    class="absolute top-[-20%] right-[-10%] w-[60%] h-[80%] bg-blue-50 rounded-full blur-[150px] opacity-40 animate-pulse">
                </div>
                <div class="absolute bottom-[-20%] left-[-10%] w-[60%] h-[80%] bg-pink-100 rounded-full blur-[150px] opacity-40 animate-pulse"
                    style="animation-delay: 1.5s;"></div>
            </div>

           
            <div class="relative z-10 w-full max-w-lg">
                <div class="glass-card rounded-[3.5rem] p-10 md:p-16 shadow-2xl border border-white/50">

                    <div class="text-center mb-10">
                        <div
                            class="w-20 h-20 bg-blue-500 text-white rounded-3xl flex items-center justify-center text-3xl mx-auto mb-6 shadow-lg shadow-blue-200 -rotate-6 hover:rotate-0 transition-transform duration-500">
                            <i class="fa-solid fa-user-plus"></i>
                        </div>
                        <h1 class="text-4xl font-black text-gray-900 tracking-tight mb-2">Aramıza Katıl 🐾</h1>
                        <p class="text-gray-500 font-medium">Yeni bir hesap oluşturarak yolculuğa başla.</p>
                    </div>

                    <% if (message) { %>
                        <div
                            class="mb-8 p-4 rounded-2xl animate-shake <%= message.type === 'error' ? 'bg-red-50 text-red-600 border border-red-100' : 'bg-green-50 text-green-600 border border-green-100' %> text-sm font-bold flex items-center gap-3">
                            <i
                                class="fa-solid <%= message.type === 'error' ? 'fa-circle-exclamation' : 'fa-circle-check' %>"></i>
                            <%= message.text %>
                        </div>
                        <% } %>

                            <form action="/auth/register" method="POST" class="space-y-6">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                                <div class="space-y-4">
                                    <div class="relative group">
                                        <label
                                            class="block text-[10px] font-black uppercase tracking-widest text-gray-400 mb-2 ml-1">Adınız
                                            Soyadınız</label>
                                        <div class="relative">
                                            <i
                                                class="fa-solid fa-signature absolute left-5 top-1/2 -translate-y-1/2 text-gray-300 group-focus-within:text-blue-500 transition"></i>
                                            <input type="text" name="name" placeholder="John Doe" required
                                                class="w-full bg-white/80 backdrop-blur-sm border-2 border-transparent px-12 py-4 rounded-2xl text-lg font-bold text-gray-800 focus:border-blue-300 focus:bg-white shadow-sm focus:ring-0 transition">
                                        </div>
                                    </div>

                                    <div class="relative group">
                                        <label
                                            class="block text-[10px] font-black uppercase tracking-widest text-gray-400 mb-2 ml-1">E-posta
                                            Adresiniz</label>
                                        <div class="relative">
                                            <i
                                                class="fa-solid fa-envelope absolute left-5 top-1/2 -translate-y-1/2 text-gray-300 group-focus-within:text-blue-500 transition"></i>
                                            <input type="email" name="email" placeholder="ornek@mail.com" required
                                                class="w-full bg-white/80 backdrop-blur-sm border-2 border-transparent px-12 py-4 rounded-2xl text-lg font-bold text-gray-800 focus:border-blue-300 focus:bg-white shadow-sm focus:ring-0 transition">
                                        </div>
                                    </div>

                                   
                                    <div class="py-4">
                                        <label
                                            class="block text-[10px] font-black uppercase tracking-widest text-gray-400 mb-4 ml-1">Pikselli
                                            Dostunu Seç ✨</label>
                                        <div
                                            class="grid grid-cols-5 gap-3 max-h-[180px] overflow-y-auto p-2 rounded-2xl bg-blue-50/50">
                                            <% for(let i=1; i<=18; i++) { %>
                                                <label class="cursor-pointer relative group/avatar">
                                                    <input type="radio" name="avatar" value="avatar<%= i %>.png"
                                                        class="peer hidden" <%=i===1 ? 'checked' : '' %>>
                                                    <div
                                                        class="aspect-square bg-white rounded-xl border-2 border-white peer-checked:border-blue-500 peer-checked:bg-blue-50 shadow-sm transition-all flex items-center justify-center p-1 group-hover/avatar:scale-110">
                                                        <img src="/images/avatars/avatar<%= i %>.png" alt="Pet <%= i %>"
                                                            class="w-full h-full object-contain pixelated"
                                                            onerror="this.src='https://api.dicebear.com/7.x/pixel-art/svg?seed=<%= i %>'">
                                                    </div>
                                                </label>
                                                <% } %>
                                        </div>
                                    </div>

                                    <div class="relative group">
                                        <label
                                            class="block text-[10px] font-black uppercase tracking-widest text-gray-400 mb-2 ml-1">Şifre
                                            Belirleyin</label>
                                        <div class="relative">
                                            <i
                                                class="fa-solid fa-lock absolute left-5 top-1/2 -translate-y-1/2 text-gray-300 group-focus-within:text-blue-500 transition"></i>
                                            <input type="password" name="password" placeholder="••••••••" required
                                                class="w-full bg-white/80 backdrop-blur-sm border-2 border-transparent px-12 py-4 rounded-2xl text-lg font-bold text-gray-800 focus:border-blue-300 focus:bg-white shadow-sm focus:ring-0 transition">
                                        </div>
                                    </div>
                                </div>

                                <button
                                    class="w-full bg-gradient-to-r from-gray-900 to-black text-white py-5 rounded-[2rem] font-black text-lg shadow-xl hover:from-blue-600 hover:to-blue-500 hover:scale-[1.02] active:scale-95 transition-all duration-300">
                                    Hesabımı Oluştur <i class="fa-solid fa-sparkles ml-2 opacity-50"></i>
                                </button>
                            </form>

                            <div class="mt-12 pt-8 border-t border-gray-100/50 text-center">
                                <p class="text-gray-500 font-medium">Zaten bir hesabınız var mı?
                                    <a href="/auth/login"
                                        class="text-blue-500 font-black hover:border-b-2 border-blue-200 transition-all">Giriş
                                        Yapın</a>
                                </p>
                            </div>
                </div>
            </div>
        </div>

        <%- include("../partials/footer") %>